/* (c) https://github.com/MontiCore/monticore */
pluginManagement {
  repositories {
    maven {
      credentials.username nexusUser
      credentials.password nexusPassword
      url publicNexus
    }
    gradlePluginPortal()
  }
}

rootProject.name = 'sysml_v2'

// Official
include 'language'
include 'visualization'

// For Verification
include 'language4verification'
include 'language-server'
include 'vscode-plugin'
