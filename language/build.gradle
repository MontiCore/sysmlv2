// (c) https://github.com/MontiCore/monticore

/* ============================================================ */
/* ========================= Plugins ========================== */
/* ============================================================ */

plugins {
  id "de.monticore.generator" version "$mc_version"
  id "com.gradleup.shadow" version "8.3.9"
}

/* ============================================================ */
/* ========================= Project ========================== */
/* ============================================================ */

description = 'SysML_v2_official'

/* ============================================================ */
/* ========================= Versions ========================= */
/* ============================================================ */


def commons_cli_version = "1.4"
def commons_lang_version = "3.11"

/* ============================================================ */
/* ======================= Configuration ====================== */
/* ============================================================ */


test {
  useJUnitPlatform()
}

/* ============================================================ */
/* ======================= Dependencies ======================= */
/* ============================================================ */

if (!isPipelineFullBuild) {
  sourceSets {
    test {
      java {
        exclude 'parser/ParsersComparisonTest.java'
        exclude 'parser/ParsersComparisonFullTest.java'
      }
    }
  }
}

dependencies {
  // language dependencies
  grammar "de.monticore.lang:ocl:$mc_version"
  grammar project(":cc")

  compileOnly "de.monticore.lang:cd4analysis:$mc_version"
  implementation "de.monticore:stream-symbols:$mc_version"

  implementation "org.apache.commons:commons-lang3:$commons_lang_version"
  implementation "commons-cli:commons-cli:$commons_cli_version"

  if (isPipelineFullBuild) {
    testImplementation group: 'org.omg.sysml', name: 'org.omg.sysml.interactive', version: omg_version, classifier: 'all'
  }
}

/* ============================================================ */
/* ========================== Tasks =========================== */
/* ============================================================ */


// Fixes issue with java plugin in projects without resources
compileTestJava.doFirst { mkdir sourceSets.main.output.resourcesDir }

// static delegate of "Log" prohibits parallel execution of tests that check the log...
tasks.withType(Test) {
  maxParallelForks = 1
}

tasks.withType(JavaCompile) {
  options.fork = true
  options.encoding = "UTF-8"
  options.deprecation false
  options.warnings = false
  options.headerOutputDirectory.convention(null)

  def javaLauncher = javaToolchains.launcherFor {
    languageVersion = JavaLanguageVersion.of(21)
  }

  doFirst {
    options.forkOptions.executable = javaLauncher.get().executablePath.asFile.absolutePath

    options.forkOptions.jvmArgs += [
        "-cp", configurations.ecjCompiler.asPath,
        "org.eclipse.jdt.internal.compiler.batch.Main"
    ]
  }
}

// all in one cli-jar
shadowJar {
  manifest {
    attributes "Main-Class": " de.monticore.lang.sysmlv2.SysMLv2Tool"
  }
  archiveClassifier = 'mc-tool'
  minimize()
}

java {
//  withJavadocJar()
  withSourcesJar()
}


/* ============================================================ */
/* ======================= Publishing ========================= */
/* ============================================================ */

publishing {
  // configure what artifacts to publish
  publications {
    mavenJava(MavenPublication) {
      from components.java
    }
  }
  repositories.maven {
    name = "Nexus"
    credentials.username nexusUser
    credentials.password nexusPassword
    def releasesRepoUrl = "https://nexus.se.rwth-aachen.de/content/repositories/monticore-releases/"
    def snapshotsRepoUrl = "https://nexus.se.rwth-aachen.de/content/repositories/monticore-snapshots/"
    url = version.endsWith('SNAPSHOT') ? snapshotsRepoUrl : releasesRepoUrl
  }
}

defaultTasks('build')

tasks.named('generateMCGrammars') {
  groovyHook2 = file "$projectDir/src/main/groovy/gh2sysml.groovy"

}
